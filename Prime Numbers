N<-as.numeric(readline("Enter a number : "))
prime <- function(num){
  if (num<=1){
    return(FALSE)
  }
  for (num in 2:sqrt(num)) {
    if (num %% i == 0){
      return(FALSE)
    }
  }
  return(TRUE)
}
cat("Prime NUmbers : ")
for (i in 2:N) {
  if (prime(i)){
    print(i)
  }
}
